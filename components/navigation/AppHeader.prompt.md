One-line: product top bar — teal brand band with the white PHIL wordmark, pill actions, and an avatar.

```jsx
<AppHeader
  actions={[{ icon: 'moon', label: 'Sleep Order' }, { icon: 'comment', label: 'Order Insights' }]}
  user={{ initials: 'P' }} />
```
`theme`: 'brand' (teal, default) or 'light' (white bar with hairline border).
