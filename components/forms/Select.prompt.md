One-line: labeled dropdown trigger styled to match Input, with a trailing chevron.

```jsx
<Select label="Select Insurance Provider"
        options={['United Healthcare CM', 'Aetna', 'Cigna']}
        placeholder="Select option" />
```
Muted placeholder until a value is chosen. `options` accept strings or `{value,label}`.
