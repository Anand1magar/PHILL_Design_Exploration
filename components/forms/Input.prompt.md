One-line: labeled single-line text field with focus ring, error and disabled states.

```jsx
<Input label="CMM Key" placeholder="Enter CMM key..." />
<Input label="Member ID" value="U88294022" error />
```
States: default, focus (blue ring), error (red border), disabled (muted).
