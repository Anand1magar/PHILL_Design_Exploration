One-line: single-select radio with a blue filled ring when selected.

```jsx
<Radio name="pa-type" value="standard" label="Standard" checked onChange={fn} />
<Radio name="pa-type" value="urgent" label="Urgent" onChange={fn} />
```
